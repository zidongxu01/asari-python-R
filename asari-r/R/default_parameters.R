# Default parameters corresponding to asari/default_parameters.py.

default_parameters <- function() {
  list(
    mz_tolerance_ppm = 5,
    min_intensity = 100,
    min_timepoints = 5,
    min_peak_height = 1000
  )
}
