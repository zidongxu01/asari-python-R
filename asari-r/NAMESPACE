# 本文件声明普通用户可以直接调用的公开接口。
export(asari_default_parameters)
export(asari_process)

# 注册处理结果的打印方法，直接输入结果对象即可查看输出位置。
S3method(print,asari_result)

# 核心算法中使用了这些R推荐包的非限定函数。
import(grDevices)
import(graphics)
import(stats)
import(utils)
